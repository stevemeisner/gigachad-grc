services:
  # ===========================================
  # API Gateway - Traefik (Official Docker Image)
  # ===========================================
  traefik:
    image: traefik:v3.0
    container_name: grc-traefik
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--log.level=INFO"
    ports:
      - "80:80"
      - "443:443"
      - "8090:8080"  # Traefik dashboard
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./gateway/traefik.yml:/etc/traefik/traefik.yml:ro
    networks:
      - grc-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

  # ===========================================
  # Database - PostgreSQL (Official Docker Image)
  # ===========================================
  postgres:
    image: postgres:16-alpine
    container_name: grc-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-grc}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-grc_secret}
      POSTGRES_DB: ${POSTGRES_DB:-gigachad_grc}
    ports:
      - "5433:5432"  # Using 5433 externally to avoid conflicts
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # No initdb scripts are mounted here. ./database/init holds incremental
      # patches that ALTER tables owned by Prisma, so on a fresh volume the
      # very first one (02-soft-delete-migration.sql) fails with
      # `relation "controls" does not exist`. Postgres runs initdb scripts with
      # ON_ERROR_STOP=1, so that aborted the whole container and left a
      # half-initialised, table-less database behind. The application schema is
      # created by `prisma db push` instead — see scripts/start-demo.sh.
    networks:
      - grc-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-grc} -d ${POSTGRES_DB:-gigachad_grc}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ===========================================
  # Object Storage - MinIO (Official MinIO Image)
  # ===========================================
  minio:
    image: minio/minio:latest
    container_name: grc-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"   # API
      - "9001:9001"   # Console
    volumes:
      - minio_data:/data
    networks:
      - grc-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # ===========================================
  # Controls Service - Security Controls Management
  # ===========================================
  controls:
    build:
      context: .
      dockerfile: services/controls/Dockerfile
    container_name: grc-controls
    environment:
      NODE_ENV: development
      PORT: 3001
      DATABASE_URL: postgresql://${POSTGRES_USER:-grc}:${POSTGRES_PASSWORD:-grc_secret}@postgres:5432/${POSTGRES_DB:-gigachad_grc}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      # Firebase Authentication (Google provider only). The ID token proves
      # IDENTITY ONLY - role, permissions and organization are read from
      # PostgreSQL on every request, never from token claims. AUTH_MODE=demo
      # loads the seeded demo identity through that same database path and
      # hard-throws when NODE_ENV=production.
      AUTH_MODE: ${AUTH_MODE:-demo}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
      ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}
      AUTH_AUTO_PROVISION: ${AUTH_AUTO_PROVISION:-false}
      AUTH_DEFAULT_ORG_ID: ${AUTH_DEFAULT_ORG_ID:-}
      # Security secrets
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-}
      JWT_SECRET: ${JWT_SECRET:-}
      SESSION_SECRET: ${SESSION_SECRET:-}
      # Backup configuration
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-30}
      DR_REMOTE_BACKUP_ENABLED: ${DR_REMOTE_BACKUP_ENABLED:-false}
      DR_REMOTE_BACKUP_S3_BUCKET: ${DR_REMOTE_BACKUP_S3_BUCKET:-}
    ports:
      - "3001:3001"
    volumes:
      - ./deploy:/app/deploy:ro
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - grc-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.controls.rule=PathPrefix(`/api/controls`)"
      - "traefik.http.routers.controls-evidence.rule=PathPrefix(`/api/evidence`)"
      - "traefik.http.services.controls.loadbalancer.server.port=3001"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

  # ===========================================
  # Frameworks Service - Compliance Frameworks & Risk Management
  # ===========================================
  frameworks:
    build:
      context: .
      dockerfile: services/frameworks/Dockerfile
    container_name: grc-frameworks
    environment:
      NODE_ENV: development
      PORT: 3002
      DATABASE_URL: postgresql://${POSTGRES_USER:-grc}:${POSTGRES_PASSWORD:-grc_secret}@postgres:5432/${POSTGRES_DB:-gigachad_grc}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: false
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      # Firebase Authentication (Google provider only). The ID token proves
      # IDENTITY ONLY - role, permissions and organization are read from
      # PostgreSQL on every request, never from token claims. AUTH_MODE=demo
      # loads the seeded demo identity through that same database path and
      # hard-throws when NODE_ENV=production.
      AUTH_MODE: ${AUTH_MODE:-demo}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
      ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}
      AUTH_AUTO_PROVISION: ${AUTH_AUTO_PROVISION:-false}
      AUTH_DEFAULT_ORG_ID: ${AUTH_DEFAULT_ORG_ID:-}
    ports:
      - "3002:3002"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - grc-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frameworks.rule=PathPrefix(`/api/frameworks`)"
      - "traefik.http.routers.frameworks-risks.rule=PathPrefix(`/api/risks`)"
      - "traefik.http.services.frameworks.loadbalancer.server.port=3002"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

  # ===========================================
  # Policies Service - Policy Center
  # ===========================================
  policies:
    build:
      context: .
      dockerfile: services/policies/Dockerfile
    container_name: grc-policies
    environment:
      NODE_ENV: development
      PORT: 3004
      DATABASE_URL: postgresql://${POSTGRES_USER:-grc}:${POSTGRES_PASSWORD:-grc_secret}@postgres:5432/${POSTGRES_DB:-gigachad_grc}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: false
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      # Firebase Authentication (Google provider only). The ID token proves
      # IDENTITY ONLY - role, permissions and organization are read from
      # PostgreSQL on every request, never from token claims. AUTH_MODE=demo
      # loads the seeded demo identity through that same database path and
      # hard-throws when NODE_ENV=production.
      AUTH_MODE: ${AUTH_MODE:-demo}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
      ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}
      AUTH_AUTO_PROVISION: ${AUTH_AUTO_PROVISION:-false}
      AUTH_DEFAULT_ORG_ID: ${AUTH_DEFAULT_ORG_ID:-}
    ports:
      - "3004:3004"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - grc-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.policies.rule=PathPrefix(`/api/policies`)"
      - "traefik.http.services.policies.loadbalancer.server.port=3004"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

  # ===========================================
  # TPRM Service - Third Party Risk Management
  # ===========================================
  tprm:
    build:
      context: .
      dockerfile: services/tprm/Dockerfile
    container_name: grc-tprm
    environment:
      NODE_ENV: development
      PORT: 3005
      DATABASE_URL: postgresql://${POSTGRES_USER:-grc}:${POSTGRES_PASSWORD:-grc_secret}@postgres:5432/${POSTGRES_DB:-gigachad_grc}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: false
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      # Firebase Authentication (Google provider only). The ID token proves
      # IDENTITY ONLY - role, permissions and organization are read from
      # PostgreSQL on every request, never from token claims. AUTH_MODE=demo
      # loads the seeded demo identity through that same database path and
      # hard-throws when NODE_ENV=production.
      AUTH_MODE: ${AUTH_MODE:-demo}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
      ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}
      AUTH_AUTO_PROVISION: ${AUTH_AUTO_PROVISION:-false}
      AUTH_DEFAULT_ORG_ID: ${AUTH_DEFAULT_ORG_ID:-}
    ports:
      - "3005:3005"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - grc-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tprm-vendors.rule=PathPrefix(`/api/vendors`)"
      - "traefik.http.routers.tprm-assessments.rule=PathPrefix(`/api/assessments`)"
      - "traefik.http.routers.tprm-contracts.rule=PathPrefix(`/api/contracts`)"
      - "traefik.http.routers.tprm-config.rule=PathPrefix(`/api/tprm-config`)"
      - "traefik.http.services.tprm.loadbalancer.server.port=3005"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

  # ===========================================
  # Trust Service - Questionnaires & Knowledge Base
  # ===========================================
  trust:
    build:
      context: .
      dockerfile: services/trust/Dockerfile
    container_name: grc-trust
    environment:
      NODE_ENV: development
      PORT: 3006
      DATABASE_URL: postgresql://${POSTGRES_USER:-grc}:${POSTGRES_PASSWORD:-grc_secret}@postgres:5432/${POSTGRES_DB:-gigachad_grc}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: false
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      # Firebase Authentication (Google provider only). The ID token proves
      # IDENTITY ONLY - role, permissions and organization are read from
      # PostgreSQL on every request, never from token claims. AUTH_MODE=demo
      # loads the seeded demo identity through that same database path and
      # hard-throws when NODE_ENV=production.
      AUTH_MODE: ${AUTH_MODE:-demo}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
      ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}
      AUTH_AUTO_PROVISION: ${AUTH_AUTO_PROVISION:-false}
      AUTH_DEFAULT_ORG_ID: ${AUTH_DEFAULT_ORG_ID:-}
    ports:
      - "3006:3006"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - grc-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.trust-questionnaires.rule=PathPrefix(`/api/questionnaires`)"
      - "traefik.http.routers.trust-knowledge-base.rule=PathPrefix(`/api/knowledge-base`)"
      - "traefik.http.routers.trust-center.rule=PathPrefix(`/api/trust-center`)"
      - "traefik.http.services.trust.loadbalancer.server.port=3006"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

  # ===========================================
  # Audit Service - Internal & External Audits
  # ===========================================
  audit:
    build:
      context: .
      dockerfile: services/audit/Dockerfile
    container_name: grc-audit
    environment:
      NODE_ENV: development
      PORT: 3007
      DATABASE_URL: postgresql://${POSTGRES_USER:-grc}:${POSTGRES_PASSWORD:-grc_secret}@postgres:5432/${POSTGRES_DB:-gigachad_grc}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: false
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      # Firebase Authentication (Google provider only). The ID token proves
      # IDENTITY ONLY - role, permissions and organization are read from
      # PostgreSQL on every request, never from token claims. AUTH_MODE=demo
      # loads the seeded demo identity through that same database path and
      # hard-throws when NODE_ENV=production.
      AUTH_MODE: ${AUTH_MODE:-demo}
      FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID:-}
      ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}
      AUTH_AUTO_PROVISION: ${AUTH_AUTO_PROVISION:-false}
      AUTH_DEFAULT_ORG_ID: ${AUTH_DEFAULT_ORG_ID:-}
    ports:
      - "3007:3007"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - grc-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.audit-audits.rule=PathPrefix(`/api/audits`)"
      - "traefik.http.routers.audit-requests.rule=PathPrefix(`/api/audit-requests`)"
      - "traefik.http.routers.audit-findings.rule=PathPrefix(`/api/audit-findings`)"
      - "traefik.http.routers.audit-portal.rule=PathPrefix(`/api/audit-portal`)"
      - "traefik.http.routers.audit-templates.rule=PathPrefix(`/api/audit/templates`)"
      - "traefik.http.routers.audit-workpapers.rule=PathPrefix(`/api/audit/workpapers`)"
      - "traefik.http.routers.audit-test-procedures.rule=PathPrefix(`/api/audit/test-procedures`)"
      - "traefik.http.routers.audit-remediation.rule=PathPrefix(`/api/audit/remediation`)"
      - "traefik.http.routers.audit-analytics.rule=PathPrefix(`/api/audit/analytics`)"
      - "traefik.http.routers.audit-planning.rule=PathPrefix(`/api/audit/planning`)"
      - "traefik.http.routers.audit-reports.rule=PathPrefix(`/api/audit/reports`)"
      - "traefik.http.routers.audit-ai.rule=PathPrefix(`/api/audit/audit-ai`)"
      - "traefik.http.services.audit.loadbalancer.server.port=3007"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

  # ===========================================
  # Monitoring Stack
  # ===========================================
  prometheus:
    image: prom/prometheus:v2.47.1
    container_name: grc-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.enable-lifecycle'
    volumes:
      - prometheus_data:/prometheus
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    networks:
      - grc-network
    restart: unless-stopped
    depends_on:
      - controls
      - audit
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prometheus.rule=Host(`prometheus.localhost`)"
      - "traefik.http.routers.prometheus.entrypoints=web"
      - "traefik.http.services.prometheus.loadbalancer.server.port=9090"
    security_opt:
      - no-new-privileges:true

  grafana:
    image: grafana/grafana:10.2.0
    container_name: grc-grafana
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: "Viewer"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
    ports:
      - "3003:3000"
    networks:
      - grc-network
    restart: unless-stopped
    depends_on:
      - prometheus
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.grafana.rule=Host(`grafana.localhost`)"
      - "traefik.http.routers.grafana.entrypoints=web"
      - "traefik.http.services.grafana.loadbalancer.server.port=3000"
    security_opt:
      - no-new-privileges:true

networks:
  grc-network:
    driver: bridge

volumes:
  postgres_data:
  minio_data:
  prometheus_data:
  grafana_data:
